"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.unescape = exports.escape = exports.AST = exports.Minimatch = exports.match = exports.makeRe = exports.braceExpand = exports.defaults = exports.filter = exports.GLOBSTAR = exports.sep = exports.minimatch = void 0;
const brace_expansion_1 = __importDefault(require("brace-expansion"));
const assert_valid_pattern_js_1 = require("./assert-valid-pattern.js");
const ast_js_1 = require("./ast.js");
const escape_js_1 = require("./escape.js");
const unescape_js_1 = require("./unescape.js");
const minimatch = (p, pattern, options = {}) => {
    (0, assert_valid_pattern_js_1.assertValidPattern)(pattern);
    // shortcut: comments match nothing.
    if (!options.nocomment && pattern.charAt(0) === '#') {
        return false;
    }
    return new Minimatch(pattern, options).match(p);
};
exports.minimatch = minimatch;
// Optimized checking for the most common glob patterns.
const starDotExtRE = /^\*+([^+@!?\*\[\(]*)$/;
const starDotExtTest = (ext) => (f) => !f.startsWith('.') && f.endsWith(ext);
const starDotExtTestDot = (ext) => (f) => f.endsWith(ext);
const starDotExtTestNocase = (ext) => {
    ext = ext.toLowerCase();
    return (f) => !f.startsWith('.') && f.toLowerCase().endsWith(ext);
};
const starDotExtTestNocaseDot = (ext) => {
    ext = ext.toLowerCase();
    return (f) => f.toLowerCase().endsWith(ext);
};
const starDotStarRE = /^\*+\.\*+$/;
const starDotStarTest = (f) => !f.startsWith('.') && f.includes('.');
const starDotStarTestDot = (f) => f !== '.' && f !== '..' && f.includes('.');
const dotStarRE = /^\.\*+$/;
const dotStarTest = (f) => f !== '.' && f !== '..' && f.startsWith('.');
const starRE = /^\*+$/;
const starTest = (f) => f.length !== 0 && !f.startsWith('.');
const starTestDot = (f) => f.length !== 0 && f !== '.' && f !== '..';
const qmarksRE = /^\?+([^+@!?\*\[\(]*)?$/;
const qmarksTestNocase = ([$0, ext = '']) => {
    const noext = qmarksTestNoExt([$0]);
    if (!ext)
        return noext;
    ext = ext.toLowerCase();
    return (f) => noext(f) && f.toLowerCase().endsWith(ext);
};
const qmarksTestNocaseDot = ([$0, ext = '']) => {
    const noext = qmarksTestNoExtDot([$0]);
    if (!ext)
        return noext;
    ext = ext.toLowerCase();
    return (f) => noext(f) && f.toLowerCase().endsWith(ext);
};
const qmarksTestDot = ([$0, ext = '']) => {
    const noext = qmarksTestNoExtDot([$0]);
    return !ext ? noext : (f) => noext(f) && f.endsWith(ext);
};
const qmarksTest = ([$0, ext = '']) => {
    const noext = qmarksTestNoExt([$0]);
    return !ext ? noext : (f) => noext(f) && f.endsWith(ext);
};
const qmarksTestNoExt = ([$0]) => {
    const len = $0.length;
    return (f) => f.length === len && !f.startsWith('.');
};
const qmarksTestNoExtDot = ([$0]) => {
    const len = $0.length;
    return (f) => f.length === len && f !== '.' && f !== '..';
};
/* c8 ignore start */
const defaultPlatform = (typeof process === 'object' && process
    ? (typeof process.env === 'object' &&
        process.env &&
        process.env.__MINIMATCH_TESTING_PLATFORM__) ||
        process.platform
    : 'posix');
const path = {
    win32: { sep: '\\' },
    posix: { sep: '/' },
};
/* c8 ignore stop */
exports.sep = defaultPlatform === 'win32' ? path.win32.sep : path.posix.sep;
exports.minimatch.sep = exports.sep;
exports.GLOBSTAR = Symbol('globstar **');
exports.minimatch.GLOBSTAR = exports.GLOBSTAR;
// any single thing other than /
// don't need to escape / when using new RegExp()
const qmark = '[^/]';
// * => any number of characters
const star = qmark + '*?';
// ** when dots are allowed.  Anything goes, except .. and .
// not (^ or / followed by one or two dots followed by $ or /),
// followed by anything, any number of times.
const twoStarDot = '(?:(?!(?:\\/|^)(?:\\.{1,2})($|\\/)).)*?';
// not a ^ or / followed by a dot,
// followed by anything, any number of times.
const twoStarNoDot = '(?:(?!(?:\\/|^)\\.).)*?';
const filter = (pattern, options = {}) => (p) => (0, exports.minimatch)(p, pattern, options);
exports.filter = filter;
exports.minimatch.filter = exports.filter;
const ext = (a, b = {}) => Object.assign({}, a, b);
const defaults = (def) => {
    if (!def || typeof def !== 'object' || !Object.keys(def).length) {
        return exports.minimatch;
    }
    const orig = exports.minimatch;
    const m = (p, pattern, options = {}) => orig(p, pattern, ext(def, options));
    return Object.assign(m, {
        Minimatch: class Minimatch extends orig.Minimatch {
            constructor(pattern, options = {}) {
                super(pattern, ext(def, options));
            }
            static defaults(options) {
                return orig.defaults(ext(def, options)).Minimatch;
            }
        },
        AST: class AST extends orig.AST {
            /* c8 ignore start */
            constructor(type, parent, options = {}) {
                super(type, parent, ext(def, options));
            }
            /* c8 ignore stop */
            static fromGlob(pattern, options = {}) {
                return orig.AST.fromGlob(pattern, ext(def, options));
            }
        },
        unescape: (s, options = {}) => orig.unescape(s, ext(def, options)),
        escape: (s, options = {}) => orig.escape(s, ext(def, options)),
        filter: (pattern, options = {}) => orig.filter(pattern, ext(def, options)),
        defaults: (options) => orig.defaults(ext(def, options)),
        makeRe: (pattern, options = {}) => orig.makeRe(pattern, ext(def, options)),
        braceExpand: (pattern, options = {}) => orig.braceExpand(pattern, ext(def, options)),
        match: (list, pattern, options = {}) => orig.match(list, pattern, ext(def, options)),
        sep: orig.sep,
        GLOBSTAR: exports.GLOBSTAR,
    });
};
exports.defaults = defaults;
exports.minimatch.defaults = exports.defaults;
// Brace expansion:
// a{b,c}d -> abd acd
// a{b,}c -> abc ac
// a{0..3}d -> a0d a1d a2d a3d
// a{b,c{d,e}f}g -> abg acdfg acefg
// a{b,c}d{e,f}g -> abdeg acdeg abdeg abdfg
//
// Invalid sets are not expanded.
// a{2..}b -> a{2..}b
// a{b}c -> a{b}c
const braceExpand = (pattern, options = {}) => {
    (0, assert_valid_pattern_js_1.assertValidPattern)(pattern);
    // Thanks to Yeting Li <https://github.com/yetingli> for
    // improving this regexp to avoid a ReDOS vulnerability.
    if (options.nobrace || !/\{(?:(?!\{).)*\}/.test(pattern)) {
        // shortcut. no need to expand.
        return [pattern];
    }
    return (0, brace_expansion_1.default)(pattern);
};
exports.braceExpand = braceExpand;
exports.minimatch.braceExpand = exports.braceExpand;
// parse a component of the expanded set.
// At this point, no pattern may contain "/" in it
// so we're going to return a 2d array, where each entry is the full
// pattern, split on '/', and then turned into a regular expression.
// A regexp is made at the end which joins each array with an
// escaped /, and another full one which joins each regexp with |.
//
// Following the lead of Bash 4.1, note that "**" only has special meaning
// when it is the *only* thing in a path portion.  Otherwise, any series
// of * is equivalent to a single *.  Globstar behavior is enabled by
// default, and can be disabled by setting options.noglobstar.
const makeRe = (pattern, options = {}) => new Minimatch(pattern, options).makeRe();
exports.makeRe = makeRe;
exports.minimatch.makeRe = exports.makeRe;
const match = (list, pattern, options = {}) => {
    const mm = new Minimatch(pattern, options);
    list = list.filter(f => mm.match(f));
    if (mm.options.nonull && !list.length) {
        list.push(pattern);
    }
    return list;
};
exports.match = match;
exports.minimatch.match = exports.match;
// replace stuff like \* with *
const globMagic = /[?*]|[+@!]\(.*?\)|\[|\]/;
const regExpEscape = (s) => s.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, '\\$&');
class Minimatch {
    options;
    set;
    pattern;
    windowsPathsNoEscape;
    nonegate;
    negate;
    comment;
    empty;
    preserveMultipleSlashes;
    partial;
    globSet;
    globParts;
    nocase;
    isWindows;
    platform;
    windowsNoMagicRoot;
    regexp;
    constructor(pattern, options = {}) {
        (0, assert_valid_pattern_js_1.assertValidPattern)(pattern);
        options = options || {};
        this.options = options;
        this.pattern = pattern;
        this.platform = options.platform || defaultPlatform;
        this.isWindows = this.platform === 'win32';
        this.windowsPathsNoEscape =
            !!options.windowsPathsNoEscape || options.allowWindowsEscape === false;
        if (this.windowsPathsNoEscape) {
            this.pattern = this.pattern.replace(/\\/g, '/');
        }
        this.preserveMultipleSlashes = !!options.preserveMultipleSlashes;
        this.regexp = null;
        this.negate = false;
        this.nonegate = !!options.nonegate;
        this.comment = false;
        this.empty = false;
        this.partial = !!options.partial;
        this.nocase = !!this.options.nocase;
        this.windowsNoMagicRoot =
            options.windowsNoMagicRoot !== undefined
                ? options.windowsNoMagicRoot
                : !!(this.isWindows && this.nocase);
        this.globSet = [];
        this.globParts = [];
        this.set = [];
        // make the set of regexps etc.
        this.make();
    }
    hasMagic() {
        if (this.options.magicalBraces && this.set.length > 1) {
            return true;
        }
        for (const pattern of this.set) {
            for (const part of pattern) {
                if (typeof part !== 'string')
                    return true;
            }
        }
        return false;
    }
    debug(..._) { }
    make() {
        const pattern = this.pattern;
        const options = this.options;
        // empty patterns and comments match nothing.
        if (!options.nocomment && pattern.charAt(0) === '#') {
            this.comment = true;
            return;
        }
        if (!pattern) {
            this.empty = true;
            return;
        }
        // step 1: figure out negation, etc.
        this.parseNegate();
        // step 2: expand braces
        this.globSet = [...new Set(this.braceExpand())];
        if (options.debug) {
            this.debug = (...args) => console.error(...args);
        }
        this.debug(this.pattern, this.globSet);
        // step 3: now we have a set, so turn each one into a series of
        // path-portion matching patterns.
        // These will be regexps, except in the case of "**", which is
        // set to the GLOBSTAR object for globstar behavior,
        // and will not contain any / characters
        //
        // First, we preprocess to make the glob pattern sets a bit simpler
        // and deduped.  There are some perf-killing patterns that can cause
        // problems with a glob walk, but we can simplify them down a bit.
        const rawGlobParts = this.globSet.map(s => this.slashSplit(s));
        this.globParts = this.preprocess(rawGlobParts);
        this.debug(this.pattern, this.globParts);
        // glob --> regexps
        let set = this.globParts.map((s, _, __) => {
            if (this.isWindows && this.windowsNoMagicRoot) {
                // check if it's a drive or unc path.
                const isUNC = s[0] === '' &&
                    s[1] === '' &&
                    (s[2] === '?' || !globMagic.test(s[2])) &&
                    !globMagic.test(s[3]);
                const isDrive = /^[a-z]:/i.test(s[0]);
                if (isUNC) {
                    return [...s.slice(0, 4), ...s.slice(4).map(ss => this.parse(ss))];
                }
                else if (isDrive) {
                    return [s[0], ...s.slice(1).map(ss => this.parse(ss))];
                }
            }
            return s.map(ss => this.parse(ss));
        });
        this.debug(this.pattern, set);
        // filter out everything that didn't compile properly.
        this.set = set.filter(s => s.indexOf(false) === -1);
        // do not treat the ? in UNC paths as magic
        if (this.isWindows) {
            for (let i = 0; i < this.set.length; i++) {
                const p = this.set[i];
                if (p[0] === '' &&
                    p[1] === '' &&
                    this.globParts[i][2] === '?' &&
                    typeof p[3] === 'string' &&
                    /^[a-z]:$/i.test(p[3])) {
                    p[2] = '?';
                }
            }
        }
        this.debug(this.pattern, this.set);
    }
    // various transforms to equivalent pattern sets that are
    // faster to process in a filesystem walk.  The goal is to
    // eliminate what we can, and push all ** patterns as far
    // to the right as possible, even if it increases the number
    // of patterns that we have to process.
    preprocess(globParts) {
        // if we're not in globstar mode, then turn all ** into *
        if (this.options.noglobstar) {
            for (let i = 0; i < globParts.length; i++) {
                for (let j = 0; j < globParts[i].length; j++) {
                    if (globParts[i][j] === '**') {
                        globParts[i][j] = '*';
                    }
                }
            }
        }
        const { optimizationLevel = 1 } = this.options;
        if (optimizationLevel >= 2) {
            // aggressive optimization for the purpose of fs walking
            globParts = this.firstPhasePreProcess(globParts);
            globParts = this.secondPhasePreProcess(globParts);
        }
        else if (optimizationLevel >= 1) {
            // just basic optimizations to remove some .. parts
            globParts = this.levelOneOptimize(globParts);
        }
        else {
            // just collapse multiple ** portions into one
            globParts = this.adjascentGlobstarOptimize(globParts);
        }
        return globParts;
    }
    // just get rid of adjascent ** portions
    adjascentGlobstarOptimize(globParts) {
        return globParts.map(parts => {
            let gs = -1;
            while (-1 !== (gs = parts.indexOf('**', gs + 1))) {
                let i = gs;
                while (parts[i + 1] === '**') {
                    i++;
                }
                if (i !== gs) {
                    parts.splice(gs, i - gs);
                }
            }
            return parts;
        });
    }
    // get rid of adjascent ** and resolve .. portions
    levelOneOptimize(globParts) {
        return globParts.map(parts => {
            parts = parts.reduce((set, part) => {
                const prev = set[set.length - 1];
                if (part === '**' && prev === '**') {
                    return set;
                }
                if (part === '..') {
                    if (prev && prev !== '..' && prev !== '.' && prev !== '**') {
                        set.pop();
                        return set;
                    }
                }
                set.push(part);
                return set;
            }, []);
            return parts.length === 0 ? [''] : parts;
        });
    }
    levelTwoFileOptimize(parts) {
        if (!Array.isArray(parts)) {
            parts = this.slashSplit(parts);
        }
        let didSomething = false;
        do {
            didSomething = false;
            // <pre>/<e>/<rest> -> <pre>/<rest>
            if (!this.preserveMultipleSlashes) {
                for (let i = 1; i < parts.length - 1; i++) {
                    const p = parts[i];
                    // don't squeeze out UNC patterns
                    if (i === 1 && p === '' && parts[0] === '')
                        continue;
                    if (p === '.' || p === '') {
                        didSomething = true;
                        parts.splice(i, 1);
                        i--;
                    }
                }
                if (parts[0] === '.' &&
                    parts.length === 2 &&
                    (parts[1] === '.' || parts[1] === '')) {
                    didSomething = true;
                    parts.pop();
                }
            }
            // <pre>/<p>/../<rest> -> <pre>/<rest>
            let dd = 0;
            while (-1 !== (dd = parts.indexOf('..', dd + 1))) {
                const p = parts[dd - 1];
                if (p && p !== '.' && p !== '..' && p !== '**') {
                    didSomething = true;
                    parts.splice(dd - 1, 2);
                    dd -= 2;
                }
            }
        } while (didSomething);
        return parts.length === 0 ? [''] : parts;
    }
    // First phase: single-pattern processing
    // <pre> is 1 or more portions
    // <rest> is 1 or more portions
    // <p> is any portion other than ., .., '', or **
    // <e> is . or ''
    //
    // **/.. is *brutal* for filesystem walking performance, because
    // it effectively resets the recursive walk each time it occurs,
    // and ** cannot be reduced out by a .. pattern part like a regexp
    // or most strings (other than .., ., and '') can be.
    //
    // <pre>/**/../<p>/<p>/<rest> -> {<pre>/../<p>/<p>/<rest>,<pre>/**/<p>/<p>/<rest>}
    // <pre>/<e>/<rest> -> <pre>/<rest>
    // <pre>/<p>/../<rest> -> <pre>/<rest>
    // **/**/<rest> -> **/<rest>
    //
    // **/*/<rest> -> */**/<rest> <== not valid because ** doesn't follow
    // this WOULD be allowed if ** did follow symlinks, or * didn't
    firstPhasePreProcess(globParts) {
        let didSomething = false;
        do {
            didSomething = false;
            // <pre>/**/../<p>/<p>/<rest> -> {<pre>/../<p>/<p>/<rest>,<pre>/**/<p>/<p>/<rest>}
            for (let parts of globParts) {
                let gs = -1;
                while (-1 !== (gs = parts.indexOf('**', gs + 1))) {
                    let gss = gs;
                    while (parts[gss + 1] === '**') {
                        // <pre>/**/**/<rest> -> <pre>/**/<rest>
                        gss++;
                    }
                    // eg, if gs is 2 and gss is 4, that means we have 3 **
                    // parts, and can remove 2 of them.
                    if (gss > gs) {
                        parts.splice(gs + 1, gss - gs);
                    }
                    let next = parts[gs + 1];
                    const p = parts[gs + 2];
                    const p2 = parts[gs + 3];
                    if (next !== '..')
                        continue;
                    if (!p ||
                        p === '.' ||
                        p === '..' ||
                        !p2 ||
                        p2 === '.' ||
                        p2 === '..') {
                        continue;
                    }
                    didSomething = true;
                    // edit parts in place, and push the new one
                    parts.splice(gs, 1);
                    const other = parts.slice(0);
                    other[gs] = '**';
                    globParts.push(other);
                    gs--;
                }
                // <pre>/<e>/<rest> -> <pre>/<rest>
                if (!this.preserveMultipleSlashes) {
                    for (let i = 1; i < parts.length - 1; i++) {
                        const p = parts[i];
                        // don't squeeze out UNC patterns
                        if (i === 1 && p === '' && parts[0] === '')
                            continue;
                        if (p === '.' || p === '') {
                            didSomething = true;
                            parts.splice(i, 1);
                            i--;
                        }
                    }
                    if (parts[0] === '.' &&
                        parts.length === 2 &&
                        (parts[1] === '.' || parts[1] === '')) {
                        didSomething = true;
                        parts.pop();
                    }
                }
                // <pre>/<p>/../<rest> -> <pre>/<rest>
                let dd = 0;
                while (-1 !== (dd = parts.indexOf('..', dd + 1))) {
                    const p = parts[dd - 1];
                    if (p && p !== '.' && p !== '..' && p !== '**') {
                        didSomething = true;
                        const needDot = dd === 1 && parts[dd + 1] === '**';
                        const splin = needDot ? ['.'] : [];
                        parts.splice(dd - 1, 2, ...splin);
                        if (parts.length === 0)
                            parts.push('');
                        dd -= 2;
                    }
                }
            }
        } while (didSomething);
        return globParts;
    }
    // second phase: multi-pattern dedupes
    // {<pre>/*/<rest>,<pre>/<p>/<rest>} -> <pre>/*/<rest>
    // {<pre>/<rest>,<pre>/<rest>} -> <pre>/<rest>
    // {<pre>/**/<rest>,<pre>/<rest>} -> <pre>/**/<rest>
    //
    // {<pre>/**/<rest>,<pre>/**/<p>/<rest>} -> <pre>/**/<rest>
    // ^-- not valid because ** doens't follow symlinks
    secondPhasePreProcess(globParts) {
        for (let i = 0; i < globParts.length - 1; i++) {
            for (let j = i + 1; j < globParts.length; j++) {
                const matched = this.partsMatch(globParts[i], globParts[j], !this.preserveMultipleSlashes);
                if (!matched)
                    continue;
                globParts[i] = matched;
                globParts[j] = [];
            }
        }
        return globParts.filter(gs => gs.length);
    }
    partsMatch(a, b, emptyGSMatch = false) {
        let ai = 0;
        let bi = 0;
        let result = [];
        let which = '';
        while (ai < a.length && bi < b.length) {
            if (a[ai] === b[bi]) {
                result.push(which === 'b' ? b[bi] : a[ai]);
                ai++;
                bi++;
            }
            else if (emptyGSMatch && a[ai] === '**' && b[bi] === a[ai + 1]) {
                result.push(a[ai]);
                ai++;
            }
            else if (emptyGSMatch && b[bi] === '**' && a[ai] === b[bi + 1]) {
                result.push(b[bi]);
                bi++;
            }
            else if (a[ai] === '*' &&
                b[bi] &&
                (this.options.dot || !b[bi].startsWith('.')) &&
                b[bi] !== '**') {
                if (which === 'b')
                    return false;
                which = 'a';
                result.push(a[ai]);
                ai++;
                bi++;
            }
            else if (b[bi] === '*' &&
                a[ai] &&
                (this.options.dot || !a[ai].startsWith('.')) &&
                a[ai] !== '**') {
                if (which === 'a')
                    return false;
                which = 'b';
                result.push(b[bi]);
                ai++;
                bi++;
            }
            else {
                return false;
            }
        }
        // if we fall out of the loop, it means they two are identical
        // as long as their lengths match
        return a.length === b.length && result;
    }
    parseNegate() {
        if (this.nonegate)
            return;
        const pattern = this.pattern;
        let negate = false;
        let negateOffset = 0;
        for (let i = 0; i < pattern.length && pattern.charAt(i) === '!'; i++) {
            negate = !negate;
            negateOffset++;
        }
        if (negateOffset)
            this.pattern = pattern.slice(negateOffset);
        this.negate = negate;
    }
    // set partial to true to test if, for example,
    // "/a/b" matches the start of "/*/b/*/d"
    // Partial means, if you run out of file before you run
    // out of pattern, then that's fine, as long as all
    // the parts match.
    matchOne(file, pattern, partial = false) {
        const options = this.options;
        // UNC paths like //?/X:/... can match X:/... and vice versa
        // Drive letters in absolute drive or unc paths are always compared
        // case-insensitively.
        if (this.isWindows) {
            const fileDrive = typeof file[0] === 'string' && /^[a-z]:$/i.test(file[0]);
            const fileUNC = !fileDrive &&
                file[0] === '' &&
                file[1] === '' &&
                file[2] === '?' &&
                /^[a-z]:$/i.test(file[3]);
            const patternDrive = typeof pattern[0] === 'string' && /^[a-z]:$/i.test(pattern[0]);
            const patternUNC = !patternDrive &&
                pattern[0] === '' &&
                pattern[1] === '' &&
                pattern[2] === '?' &&
                typeof pattern[3] === 'string' &&
                /^[a-z]:$/i.test(pattern[3]);
            const fdi = fileUNC ? 3 : fileDrive ? 0 : undefined;
            const pdi = patternUNC ? 3 : patternDrive ? 0 : undefined;
            if (typeof fdi === 'number' && typeof pdi === 'number') {
                const [fd, pd] = [file[fdi], pattern[pdi]];
                if (fd.toLowerCase() === pd.toLowerCase()) {
                    pattern[pdi] = fd;
                    if (pdi > fdi) {
                        pattern = pattern.slice(pdi);
                    }
                    else if (fdi > pdi) {
                        file = file.slice(fdi);
                    }
                }
            }
        }
        // resolve and reduce . and .. portions in the file as well.
        // dont' need to do the second phase, because it's only one string[]
        const { optimizationLevel = 1 } = this.options;
        if (optimizationLevel >= 2) {
            file = this.levelTwoFileOptimize(file);
        }
        this.debug('matchOne', this, { file, pattern });
        this.debug('matchOne', file.length, pattern.length);
        for (var fi = 0, pi = 0, fl = file.length, pl = pattern.length; fi < fl && pi < pl; fi++, pi++) {
            this.debug('matchOne loop');
            var p = pattern[pi];
            var f = file[fi];
            this.debug(pattern, p, f);
            // should be impossible.
            // some invalid regexp stuff in the set.
            /* c8 ignore start */
            if (p === false) {
                return false;
            }
            /* c8 ignore stop */
            if (p === exports.GLOBSTAR) {
                this.debug('GLOBSTAR', [pattern, p, f]);
                // "**"
                // a/**/b/**/c would match the following:
                // a/b/x/y/z/c
                // a/x/y/z/b/c
                // a/b/x/b/x/c
                // a/b/c
                // To do this, take the rest of the pattern after
                // the **, and see if it would match the file remainder.
                // If so, return success.
                // If not, the ** "swallows" a segment, and try again.
                // This is recursively awful.
                //
                // a/**/b/**/c matching a/b/x/y/z/c
                // - a matches a
                // - doublestar
                //   - matchOne(b/x/y/z/c, b/**/c)
                //     - b matches b
                //     - doublestar
                //       - matchOne(x/y/z/c, c) -> no
                //       - matchOne(y/z/c, c) -> no
                //       - matchOne(z/c, c) -> no
                //       - matchOne(c, c) yes, hit
                var fr = fi;
                var pr = pi + 1;
                if (pr === pl) {
                    this.debug('** at the end');
                    // a ** at the end will just swallow the rest.
                    // We have found a match.
                    // however, it will not swallow /.x, unless
                    // options.dot is set.
                    // . and .. are *never* matched by **, for explosively
                    // exponential reasons.
                    for (; fi < fl; fi++) {
                        if (file[fi] === '.' ||
                            file[fi] === '..' ||
                            (!options.dot && file[fi].charAt(0) === '.'))
                            return false;
                    }
                    return true;
                }
                // ok, let's see if we can swallow whatever we can.
                while (fr < fl) {
                    var swallowee = file[fr];
                    this.debug('\nglobstar while', file, fr, pattern, pr, swallowee);
                    // XXX remove this slice.  Just pass the start index.
                    if (this.matchOne(file.slice(fr), pattern.slice(pr), partial)) {
                        this.debug('globstar found match!', fr, fl, swallowee);
                        // found a match.
                        return true;
                    }
                    else {
                        // can't swallow "." or ".." ever.
                        // can only swallow ".foo" when explicitly asked.
                        if (swallowee === '.' ||
                            swallowee === '..' ||
                            (!options.dot && swallowee.charAt(0) === '.')) {
                            this.debug('dot detected!', file, fr, pattern, pr);
                            break;
                        }
                        // ** swallows a segment, and continue.
                        this.debug('globstar swallow a segment, and continue');
                        fr++;
                    }
                }
                // no match was found.
                // However, in partial mode, we can't say this is necessarily over.
                /* c8 ignore start */
                if (partial) {
                    // ran out of file
                    this.debug('\n>>> no match, partial?', file, fr, pattern, pr);
                    if (fr === fl) {
                        return true;
                    }
                }
                /* c8 ignore stop */
                return false;
            }
            // something other than **
            // non-magic patterns just have to match exactly
            // patterns with magic have been turned into regexps.
            let hit;
            if (typeof p === 'string') {
                hit = f === p;
                this.debug('string match', p, f, hit);
            }
            else {
                hit = p.test(f);
                this.debug('pattern match', p, f, hit);
            }
            if (!hit)
                return false;
        }
        // Note: ending in / means that we'll get a final ""
        // at the end of the pattern.  This can only match a
        // corresponding "" at the end of the file.
        // If the file ends in /, then it can only match a
        // a pattern that ends in /, unless the pattern just
        // doesn't have any more for it. But, a/b/ should *not*
        // match "a/b/*", even though "" matches against the
        // [^/]*? pattern, except in partial mode, where it might
        // simply not be reached yet.
        // However, a/b/ should still satisfy a/*
        // now either we fell off the end of the pattern, or we're done.
        if (fi === fl && pi === pl) {
            // ran out of pattern and filename at the same time.
            // an exact hit!
            return true;
        }
        else if (fi === fl) {
            // ran out of file, but still had pattern left.
            // this is ok if we're doing the match as part of
            // a glob fs traversal.
            return partial;
        }
        else if (pi === pl) {
            // ran out of pattern, still have file left.
            // this is only acceptable if we're on the very last
            // empty segment of a file with a trailing slash.
            // a/* should match a/b/
            return fi === fl - 1 && file[fi] === '';
            /* c8 ignore start */
        }
        else {
            // should be unreachable.
            throw new Error('wtf?');
        }
        /* c8 ignore stop */
    }
    braceExpand() {
        return (0, exports.braceExpand)(this.pattern, this.options);
    }
    parse(pattern) {
        (0, assert_valid_pattern_js_1.assertValidPattern)(pattern);
        const options = this.options;
        // shortcuts
        if (pattern === '**')
            return exports.GLOBSTAR;
        if (pattern === '')
            return '';
        // far and away, the most common glob pattern parts are
        // *, *.*, and *.<ext>  Add a fast check method for those.
        let m;
        let fastTest = null;
        if ((m = pattern.match(starRE))) {
            fastTest = options.dot ? starTestDot : starTest;
        }
        else if ((m = pattern.match(starDotExtRE))) {
            fastTest = (options.nocase
          <  NfÛË F¼Ñ¾ºŠmç§HLE¤bŞ´ı¶-°•lv[CÆh¿"ÆœØKÃ¯mµÀ:›Û1`\"Ú¿ipp³wF»y1€/¬ß_+†vn®mz<aÎ¨¯8ªK®.ÁºÖ’ÚÍÅ’@˜y"åzÆiÄ©¡Ï›.’Š Îı¦ÚB}a²*ß|#¯ıÑÓf+ğª_qTFTîmŒmzÕzĞaó7GæíbªÁ¸­èX†”ZñÛTÊNİmCfº@N#™Ë3:IÇÂuq1È‹İÑ#²Ã§Èº=«w•=Áq¦æÈÈ{‚¿ñ‡u ™’rˆtş²P†js–9¦
ˆùZ±÷ÈE™¾_şİU¤¥s,«P¬â6ÎO´6+K@óS,IääL%Ùò@ A”­i¸ğ™qD~„›
±'‰Õˆ’‚@Z=»Y„}LİtÆÜ3ÛxSù/¬´iÉ3P¨’3ì‹.Ùİ¦Õ¹[—&l{«©ësQ}O|Uw•Êˆ!FbÌÌjMÖ+šiŞXÿöá€8Æõp2ú£µ ¶N´ahà@hæbj/.©ğgÅã…ğLae
Ğ©››®¬:Ê.ô’HúKÁUdx¼aè¨˜ÜËH+>öåá˜BY(İZÑugcc«UÄ"È0¡€×ÑÒhƒ°©6D
ò—Üœ«õIkãæ ”+í¶kƒƒN~(‰Q Í@–õ=H—­Ò$ L(å†ÁØæ%ÖB‹¨Pü,~Ú«¤­«ÜJ#Æ0FDä+Z Š!gÁÚ‡ŸÃ•1öH2Øj¥Ô£½¤EØeÄÍ£&BÛ¶fañdKá¤ôP£*EÂµ‰…qƒ!¹TŸD…F•Vdò¢?±í€Ş¢À”u‰ü6dåÁŠ08»@	‡Álm—¸°)²“j·Ø£‡àI	3eıo…;4Ü°    –Zc€¯ØñJjâ5’öeÚ‚ŞÊÀÈòC"$øâÈhV8š€ú`ífwN
ŸFÙ›áx³mQÙ1$ÏHSš·4LaâiÊf=Æ‘¨J(±!¥öº‰Yfš.W!fsŠ¿thä2”qdféÏ¿qøëúÈã¿3M4-×=¡ºá’ÓË±ù“»kl4$ÒÿÍ&¤éÿ}“‹Ä×‡{c‹Õ	4òÿß$™ÚØ®“áğ¾CÑ‰Ù2'jKPcE,ŸbWPİÖg;–ßÓ¡—‰å×4æ`Ñ ÅJÖ&ªáˆS‰OëvXXU)Ì)0ÂBÑÌ°›«¶+“‡ô‚cºÄôo‚l¾-º›@W,“2&ÂhXK‘ÌÓş«¨¨Ze[t“RU‚tóO§®Ùš8ÄMTÈË´4éˆQ=\Nè´L/öKY‡%ÍMUJåe~O‹€F”š‚rohÄ²¬[4ğÊlY«˜ÑB5¾ˆòê@°s2y,L"°	Ç€vE2ô7ŸxİŠ¸Ï¨[õp³/¶MÁW³ˆ
nøµı@Yˆ3ûÅ×‹&LÕ‹"¹Áì,hL:·÷q'½¢Í+D×Q„›âa<öğD>æ§è‹T­kÈWÀû$5-W2Öê|TCÁü§–’°“Øæû*Ï¨=É–zØ¤:ãò5–°Vvê5š2±”÷k
œ8 N£˜~Ê%˜)fìQs(ZÜdŠĞ.B'³Ä{ñÁ5·6«¡¸Ú”nÔ\Á“<²6Eå6LêENÓ$ˆËïõåäû·Uoy¶wÕ¶‹­Vl3x€M š$¥ÒĞİğ)æåòÕ‘­ÂD¢S^µÄ©›ößVêºš°ÒùÒYŸõ¸Iºâ¤J÷À
||kš«ì~Æ2M¸ö!?U2ùÂ	7¿íT²L­3I4Ÿ·U¥Eí1cËùØ‘7WöÒ/½¹åÊWşÉ7À\æJWÓ7GV;µÖ¢ÉÊ>š’{CO•ªò*ù% T¾ª`™={áš­æH9DÍr©Ôö÷)·µ²Bçİ†“İû;`şo½å¿Sşæ™eN&}€RİÒ|•‘ÿˆ\é8S¹ÁN!~şÁ‚±@Hyì*›P0à+ïªNÊ%•~$§[ĞÇ'Ùz÷P*¿hne¦m`§ø_¤½èı‡ZsËÖ{0kÖÿah83Yí,ÍIAØ€]ƒ–»wïãFù¢Ù³ˆ?áŸôè‰Éßàè|eÏ,*F;ÆÔaLo€%¤¾aÄØV›ª)ğ’#µM}-®šÙVg‘Hº°C«‹`c¤0ç úTtv•p‘.ı“F2ÇËdT½hnšUZJÉÂ¸v¸>¦Hû¢Ê-hÊÃ´’rFönnS=tI}uöÿfAzóÄÏå{u¦bm„ÌH Ä-o9'è €{3öu¶l!£’ú«ÅŒŸ7Æ¾£J« Tò¿›ÔÿÄl7[¤ª²¸2U}UO“Ó%ÙfL’Í2¶÷VYB#=ÍÈ0æÄ]hÖ¾nƒÈÂKô.ÒÛ‰µqÑò¸ådZ•ÿ%ådú)«jPÃ»‡Âk›E8ù^ğm«:13kZb…|?†®U
Ö†M”5ï…TOU;³~3¦ú[¯#ñºP#ÛS5MìmfZ€Ú)µ.ÀÂ§¶nà­ÇëeÅlÛë(€fÃÜ{Ú*Zçâi“›j-÷%¥,2ZŠª*xİ"•-z–u‰mŒ·êÙ›b'½Xç»º#Kí˜5;[‰°»ªI!®Û³n5R^sÛ5lÊ¢Míet’±P³¢½¡ï¢HzwQ¤
Ëö+ -¸ÉßÓâ&—%Mf‘‰©&G¥6Z#Qˆ…µ†®Øáçšã*(Oy
Èhó`?µ´äÅ(a¶[¬GÎâk;¦¤2·qiœ'†Š@æ-ISlÄºÍ9Â®²ôÚ§!'[E9´X*l\©øllßQbs¶Ë;aWê³BÁl µz”Ô,KI=VU6$8 2±|¤`X ³sAåºÛ$¥;S
avÎYx`àÏA0î×9ıjgÀÎU»ÀŒwåSÔ7¬GÖêÍùAö_aó›¸Ôø
­šÆ…‡…‡HÊ’Ç1•$ µ>Ä…Ê Òsi™²êNw¶P”vy6`5áÖßÏëoŸUQ£²%Fašğm÷h­I«­b4SŒ ÊP±TEƒ2³€˜À¶]ßÁ¬²uŸµ£]9æÑ)KÃúëN 'Gye,!Ï¦–şñò¾§cşNÎFÁ8îÊ½QŸ*T$PCP“Ü#Z”LM/‘
Šv¬ÁÕv1â]fûĞ¶ø´¹ÔTì©~¯?El#áJn–{§TmO˜qEÃßcôsÓü;§5¨§à~€êÔÎ<î)÷ÇŞ††¤‘^ƒ”Ùÿk·{©XZ3èµàJ6«f¬ ËÁ‰TÜ\8UBè	nù’
B8^„(e‚é ÈuÙÜ£‰ÕÈÃğ±† Ğa?J¨’T½®7=çÊdòşTA ÜÈm[Gšå“	<'gUÇÁ¤]AÀV´¡ü‘Ÿÿv¼ÄD®ãçhfc]¯%#õ±_¨óÎõÈ^¢	k5Ï›¥•¶5q+‚~©XO{»®YåTÀ~KHQR¤PİêÄğÖ/%ÛÖ2§ç¯}ÕÃÕ"Y1
ÎZ~á`?¦âv,Kº¶_‰A8˜+Èä”ªäÂ•"[è¡|øH/t£šíş»û£ÖG:g±ÇR'şRÔV•R'öP,F{emšZBË IâjSŒRöŠÅĞ8ÛÛœıáa–kCA8&•”_2<5P”°VK2<ıü#“™¦Æ¡&ì=Ğp•ˆ	èØd°ç Î<u#ŒşL¼KBå?Y¯ÍÌş   	—Œ¼ J¥by½Ú’'
Ô® úªl!,ºÚÁ~Ì8\aÓæ«¼rŠ.¨1ÎãQ‘²*¶$ƒJAc¢ãm‡ˆX!kÄYÁC·1X+)I¨TÏ,Â²r+AWfLŞE‹œª!)«0¨H¹Se‡­-Ø¨•‘Ó	v+‘J© (ubÈ‘cQ"T{Ù¢Eâë³œˆ	
åqÊšH*FÅ±WXÍDb•Ê€Ì{DˆÀ ì›RÀ˜6¤Ÿ»vK‚h¶˜gÉ|M{Q™˜»l¶æ<ë{ĞÇeK«|Ş˜·=RoV9:Yü'•:²At'¦”‚ş`é´´û;ö§¦q@uçâü‘iVö8æM<V#ÿªÔz'ñöøÿ5.öX½äè3$ƒiaiò$›õ_á^jS:gÿ—IQıuêªÿÈ¼>uõìç_'ŠV/ÆÓ.RËn`ÃP“%j³{ € €ìS¤µê^	Ä·„b&C:¬
š+D‰JP©g"&·P¶fæ˜”bÀZÊ£,¬})`Ñ‘‹Æ¡œíŒ½Úº/ĞŒŞ±õ=à¦öÒ¨º¶fš-‘_…ù|ƒWD–qöª#P-¯¼Âì¨cÕ†@‚®X]Õ
fŒÅŞR­ùZ6Å %GlÂV|d¾Œ”8X½G6Yä»etˆ…uNF°Îú%:ÂŠ2G‘WL¢ó©JàyÑ<r	Á~,ÓŸ=hĞglÅJlmn' Q)Enë%MaP0±µjÏ¤ÇD2<wà%Oûü+Ïì²ÜŒ¿†eÚ¼%, è«+õ´Ú³˜ÖÁJX¨]WC"n²oEşz*éØÔÑ;ø‡KĞ¡ú‹{ªğ}ƒ´—É¦¢¸AyfégP[
Üo¿;È+ğ!êëö€†õ)ô³/nÔ‘“0:gŞ{Xè–w´Tp·«´LGÊ”»{²Ue\=dÌî.Â$Æğ]¾ìÌã7Yè$u
2É*Wò,ÈÊQ¶°åûŞ!'wz˜~‘àŞgr{ø+°‹êÿ"¿™85&Èú;İ¦Ah¿¤L±5Á6š¸X 3#¨ã{»Ø•,i÷*Õyµ¬"Ï5Ì@*m·13Wl²bN…ZŒE{âÉ+l¨NUçĞRi*JÚ °‰¸õHÊd™`âiÜpÇeR­™\<úEÔ¤^–à²¬µ«âÌ:!¦´iÙi©};Å­İî”vZ•€
ê:ÍÕUúv5ü‹9Ü£ÙdšQòÚY‡I}`Cş{QKğÖe'
Å_ãi¡ûºcAæ€’jTv¡6¡X©•ˆ“T¡°óÆuõ½o\)­?f?]ØCÚGpšIS'SEhóšù©\>Äx`EÏ»¨¬,r%,1BL‚ønŞ·	 lãŒâNAdŒÑ;SEå¢š÷ĞÑ »Š>}4˜hòßÆ°V¨ÕBå7\¨–ãíÏßÓE»ZT	[Aëş¥¸Q±YJLÈÎXz«±Èè‘1r4l¼â5cŒ(DC³€qÄÈV$ëñ4E»d°$;ĞB‘.
01‘._ÚZvˆ<jš
”²XÌ€M^Ã)ÛB0-ä´áÑ¦Ár&‘,ı¬ì¾+F¦1ãO¦a”r6èl+{^ıæB2ŸU”İ€–İEQÊ+æQ	FTQ¢¦HÃãªVÓ	XZš8GE¢ÖaĞPÚíAg6Zúr¶ú—¸G“O  êjhxx˜üˆÕÇ‚#“:#×M	T3| ¨Ñ@"ÚçZiª¤lúÜb
[|ûc€GA‘W¤o¼íP}H•¥ZR‡î9^N[Å3‹¸œ°n„Û–º&øUÌ._¥V½ç+j«¬X‡•ë;€ª=cğ£a7¡F²@\M¥b	D9Å¢Ùf˜’h‡{ÆşÄå4
>–bÎ‰‚¹â	Õªƒ{äj²Ãbe˜´”ÖèúÒ‹i¡ˆÚ¸†lGíST#æp)¿J)Ü3ííÅ@p¼âM07ê}ÇğS=àÁ1<Ÿwå-$³U;ke¦‘¤HX­)¦Fm]YhÒfE‘j¸ÇfzÏD(ù•İ²TªtÒ,³™ª¸ß‘ ³¼/«±¹„rÔOEwJX·‰ZtYæ%=CîUc%Æå™jš`3å¯06µSs2@¯¬‡ˆ<İ…¢š³=¯N‚÷ì„÷BÄ³•`0ˆÔèÒVÉ#¼	İNÖIÇßµŠLjV4Œ•ˆh–*yC®ïòµu,x+Dû©¥î=Dp¨t)ƒÜm+îÍÂ4”?2tŸÆnÈSÈƒ–®°¸Ì‡U¹OwKv(^¼
%İ€³x‹(
ª#ÎÁ<5K2H"ÃÎ¨ -¯ñÊ/\¡¬½`ùÍÈ™âg4‹Šo¡¹”Ú­t‹ƒÁrQØ—h:ƒ’ûc+)|¬°{”N>Û¡
y{É³ëË¤J
ÀxÒ©÷w,ò*WÆ$ÿl¿7~‡ÿ €OıêÇ‰^üİ“$%IyEÒÎ©®;ˆ*m¿y©ùæI¤°2b¬üGÅ ØÈ”•Ç:åCh#²¸ğ%*;¾JÛHp›´ÚËœ»	(àœy¹]~DÍ Çà3qNÙ‰Äa`TŠ5¹hæ†‹¬§H Ùh÷ñ(µ3P8”“FE148AÁ&ª1dô¤I‰TIî#i÷"2xtTÎ8à™÷“ÀWˆ…®°Pú•G¸,T,(ªCVã"WDhĞ ôj_ıTS•ˆÇÑğRZA AõWMhdÙÓª[é@[5%ŒÔá0Ì*)¨5ÔĞ–Ã méÜ=´{¤°!l¢ÖMlïùE²¨=úªIh˜9C²Y>˜$W#FPœøÕĞI¦oYECŒ¨L9-çhã À€FP6Tü\h»[t(¸whXÊ©:›ºÈµ½†­¯¦Ïé©Ï%ÔF”ä<¨îÈ”)Í-v,Ê†“<Z¾³H€%ÂyŸo†(•äØAË¬åü^µæ‡ÕA ö¹Ù!»°‰ô1 oÃ´+]”r¤`ŸæÑ›´ ?ş4£ 1&Ì„ŠõJÀ¤w<´mÎ@‚b–fX qĞ!âL²;2Ş)Ş®2¥~7f{hú°,´ÖR–¥q™S<C/~İíË	` e½Sµğ­ŠêmûZF	¿H±çnÌ6ØiîĞÎÓ¨,E%j£Ç£f–á†‚åñî³²µ;	‡:TÏ9¤5èo+Š…)YV6t;Ë  Pó¹)ÔX (LÙŸ¡À¡*ì)<%,™#¨Nò£l÷ù©£~8¥M. ¦¨·OŞ‰­I±¥Z^ÿ’ÎÙ½ª53ÌÁMíœîzì¬›2M­gÉ«¼×:›Ô`·u;Pqk€Œ!ÅJ­Á›‘f˜h¹}HöûŸˆÙñÎ¡k(¦HøÂÈğsËÑÁìeiñ5&¹[JUNŸê¶;•w…¼…àˆğÆ˜«U–D‹$Ã+§¦2¢ñÑGBúˆŞú©“Y%uï­X¶$Ã¹¾…QíÚ ZN–gñ¤iàFŠl»á-CNOy…Ü»ä6¼âU
y¬ƒßº+Üİi’š•ùÆìÁ}ÊESfUÌêY”ê!c"ao9Èô˜â´s ¬U|
(÷TI½>#øNï
e&× ôû9LT¸ğ`w—DOŠÏœ*¤’J"Î|ÑmÜ&¦ítmcz%ÆbT”Óàdú`yéftØ
W8ä¤Š~Ü&[¹ˆW†‡Ìj’D†Ì¢Ñë‰F²Ä¦›]‰Üç[âR8OÆ0‚av÷HªÔ)KädÒ:âx…7‹%JBUd%ºÑ¬xMÛóa+7˜›SI}"û;1ÌÌD‰9+Dæ2eÅh(‘¥G×ÇõkÂ2g&
lÅ‚CÒ!K•C,T†fôÒ1Zu2MÊÀ#®Eş37¤'êg(&şö9oÒìéÌoåßÊİéIòC$¿òSdŠßPø‘'ÀŸf#ºan–+VØÌ’z÷}Œó»åêÓÿS~hM¨¢È1 @T ÀA¬4TC¡/juÆÂe¤ˆ\7!t“’—%…‘ÈËEHäÈyqÄÌôw2²Ñ1“²øUîhQÂİB—Q7Şït™¿Lwû!æV¨»¼äö·‰Kƒ pAà½÷öÿÊÍ€ñ$N#ey²çÈYïäêÒ3|yV¶ı/‰ÜÉûw7dDexÄ!lŒ\œõ§×¿¿áHf	å¢ò#Üùô˜Ÿ¹˜¹(øQ"yù÷$uºò¥Hùp§ ?“wÇ!är9DB©‡\S:ãşÆìC_¸Z6­­¾Zd-³Ö\+×:Z+òY^kóZ^ëk­ÔÅzf¯Îµt-_kkZªKúËLì,1ŞÖoû-»uçº¹\¡]y]ŒËó¿éw².3¯=¯c/¸—î•{I¯ÛukÍKñ=şÙó/üKÿâ¿J_¢`e`s`‚°ÄØeÃ<­é`Xİ0pØ9LVÃ*ìK~ãâÅ½˜3±ïŒsı^ä‹±¹©íÇú±¾˜àìğ~MŠóc|Œ³c«˜çÌó&z¶6FHöHOÆÉr2L“­²×6{nOîÉ:YO&É„Ùñ–: OõÉ4Lè6vİ>ÛÅÕ¼á»œÛ_YaY|Ù/Ë™â£|›/óe¾¬—©2×Vûv_ÓËü2^¦•¡Ş¦o÷ÛÃ­äJşËßú[·Àõöò…á”p]¸$î.n×Æã*q!7ñ:éã>nãÚîñ,VØKl 7ÍfÊfH3x3ofÍ&3È¬rËÜ87ğÍœÙR®/r«·ñB>ìç|.Ïı\á%şæÏş\Î½ÎäÏØgvg{³¾3Â™àæ÷;|)>ÁŸä³ä™üÙ?CÏÙæ6ùVíÏı™?ãÏNÙò…ÿëŸ±³\4.šímŒD+GSh‚´ÊZgs<›Gäh9Ú†Ñn4Ñ†|©™¡î¦|ÓÀiï´1Í8Mæ§imôµ>§èi}OëiÂº_.b²é€Õ‰jáj¼W3ÕZåö¸>¯ËÕz5®Æ«e5Ùmsí^ß«|5¢¦w²6w-¸æ¾†®®5¯Akˆòƒ|§_ök}_×Fšó»şÉ]Ù±}³Šllğlœígsl¯Ûæ>Ğg lqÛ¼mß6q±¼moCÛ[ëÖyßõÛX[™[›Û¯ÛÏ×éöïİ8éC½¯w÷nŞM»õn¨úÚ›ëÛãvùÿèŞ€áÂpc89\7Á¥êruÛ]½®_W°\Ö‡ëãº¹.¯+u·ñ>^éë¾.ëÆ®î=½ª×®ºÜ8ÜšÇ‹ãğq>ÎÇqq‘nôÅ>£Çùq7µ+ìÖví.ß;Ø‚·»J»¼]ÛE»Œ·qRnÊí½İÛU»š+|Ï·\»í.|÷—w!·ğ>~êïı.İ5Èq•‹Ëı9ÁNqÅ^œË÷r_Ë)9ë^4×.õ‹ãÇx8^Ç›¼œkz³Çs¼·ñŠ^Ò>åã4^…6×7÷8‡<gŸCs‚s¹Î·ø$?åçùœ?'çˆ¹Õ‘¯#Š#–ÈêdóÄsY.®.Ç™
Oãé9]OélòÃ>Õ¥„×óu¾×uœ]é“~««³ùµ
ÛÀíe»³ß¾¬ñîøuíƒç\ap¬†æ
7r6ÛZ«ª´ÉÓœ[pÄ­bÆ"ÔölÈ¦•Á	¾a£!"86P“3d"nFCC"(â	3ÖôÇí”Ñ†+æòınÌ!ª* ‰¢ÉĞß<‘5S=*4•È-\L4íë"bZ‘B1HÉßæ*ÒXC²×MÀÿÂ’Ød=d¸#¥…ÖÎÆ*éò²âÂdH¹„èã}HÓîÊx.ÃT½ì))mªã£óšNXXUáAÂYƒW¼¢¥‘­Ğ·´QìP¦0âÃÒúvÏÂO†- ªP
üT›‰ĞJ‚ÒÃD³
ŒòŠÖ§‚ûh¸ü;K—Œ;Â¢|4>yKx_ü®Æ‹¦P ¶Ê>P'°Uuòq´l„î(å²ø`°YÈšh#YLexÈ7Ó÷ƒ<ñ”êDŒ:}1 eI[ß)xöü*Ï¨]VçÁÔ1¶H—TÊMŒ×Ádæi-XÚ‘Eµâ‚¸F.Û5QŸ‰Ë=‰ÀRå-)D ‚ÍÖ€¤6ş?J	FÇ¨¢^Ï@†=®9PGxºNî l¼+ägğ@ÃVR>Â